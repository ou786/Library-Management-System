// books.model.ts (create a new TypeScript file)
export interface BooksModel {
    book_id: number;
    title: string;
    author_name:string;
    edition: number;
    published_year: string;
    // Add more properties as needed to match your API response
  }
  